# ## Utils
# check_candidate_data <- function(data){
#
# }
#
# check_donor_data <- function(data){
#   var_names <- c("d_id", "hospital", "recovery_day", "male", "race_eth", "hgt_in", "hgt_cm", "cod", "abo", "age",
#     "don_org", "smoke_hist", "gt_20pkyr", "don_dcd", "don_util", "organs_avl")
#
#   var_classes <- c('integer', 'numeric', 'integer', 'integer', 'factor', 'numeric', 'numeric', 'factor', 'factor', 'numeric', 'factor', 'integer', 'character', 'integer', 'integer', 'numeric')
#
#   ##
#   data2 <- data[var_names]
#   if(!identical(var_classes, sapply(data, class))){
#
#   }
#
#
#
#
# }
